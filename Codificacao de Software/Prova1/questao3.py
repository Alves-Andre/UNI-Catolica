lista= ['a', 'c', 'e', 'g', 'd', 'y', 'a', 'p', 'y', 'n', 'j', 'h', 'm', 'n', 'v', 'c', 'i', 'o', 'q', 's', 'd', 'f', 'j']  #atribuo a uma lista chamada "lista" os valores que a senhora pediu para fazer a verificação do programa
if 'x' in lista or 'X' in lista:    #verifico de a string "x" ou "X" está na lista, verifico se a letra "x" está na lista tanto em minusculo quanto em maiusculo
    print("Letra X encontrada")     #caso esteja imprimo a mensagem que a letra "x" foi encontrada
else:                               #caso no esteja
    print("Letra X inexistente")    #imprimo a mensagem que a letra "x" não foi encontrada

#Uso para fazer a verificação o "if" (se) com o "in" (está em) que verifica se algo está dentro de outro algo
# if 'qualquer string, numero ou variavel' in (está em) 'outra qualquer string, numero ou variavel':
    # a ação que eu quero fazer caso esteja

